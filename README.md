# 광고 레퍼런스 수집 시스템

메리츠증권 + 경쟁 증권사(미래에셋, 삼성, NH투자, KB, 신한투자, 한국투자, 키움, 토스) 광고를
Meta, Google, 네이버 브랜드검색/파워링크에서 자동 수집하고, 대시보드로 보여주는 내부 도구입니다.

이 저장소는 두 개의 프로젝트로 구성됩니다.

- **`ad-collector/`** — Playwright/Apify 기반 수집기 (Node.js, 백그라운드 실행)
- **`ad-ref/`** — 수집 결과를 보여주는 대시보드 (Next.js, 정적 export)

---

## 전체 구조

```
[ad-collector] 광고 수집 (스케줄러가 자동 실행)
      │  ad-collector/data/*.json 에 저장
      │  (이미지·영상·스크린샷은 ad-collector/output/ 에 저장, S3 설정 시 업로드 후 로컬 삭제)
      ▼
node sync-data.js  (ad-collector/data, output → ad-ref/public/data 로 복사)
      │
      ▼
[ad-ref] Next.js 대시보드가 public/data 의 정적 JSON을 읽어서 화면에 표시
```

ad-ref는 `output: 'export'`(정적 export) 모드로 빌드됩니다. 서버에서 실시간으로 API를
호출하는 구조가 아니라, **수집기가 만든 JSON 파일을 그대로 읽어서 보여주는 방식**입니다.
(단, Claude 인사이트용 API 라우트 2개는 예외 — 아래 "인사이트는 어떻게 만들어지는가" 참고)

`sync-data.js`는 자동으로 실행되지 않습니다. 수집이 끝난 뒤 사람이(또는 별도 cron으로)
직접 `cd ad-ref && node sync-data.js`를 실행해야 대시보드에 최신 데이터가 반영됩니다.

## 수집 대상 및 주기

| 항목 | 방식 | 주기 |
|---|---|---|
| Meta 광고 (키워드 "증권", Apify 2개 계정 포함) | Playwright 키워드 검색 + Apify | 주 1회, 이번 주 첫 평일 오전 11시 |
| Google 광고 (브랜드 9개) | Playwright (Google 광고 투명성 센터) | 주 1회, 이번 주 첫 평일 오전 11시 |
| 검색광고 일반키워드 (네이버 파워링크, "증권"/"주식") | Playwright | 월/수/금 오전 11시 |
| 네이버 브랜드검색 | Playwright 스크린샷 + 랜딩페이지 버튼 링크 파싱 | 주 1회 (평일 13시, 이번 주 첫 실행일) |
| 검색광고 브랜드키워드 (브랜드명 9개 자체를 키워드로) | Playwright, 브랜드×기기마다 새로고침 30회 | 주 1회 (평일 13시, 이번 주 첫 실행일) |
| Meta 브랜드 9개 순환 확인 | Playwright(Apify 아님 - 과금 없음) | 2시간마다(짝수 시) |
| Meta 이미지/영상 채우기 | Playwright 상세페이지 방문 | 2시간마다(홀수 시) |
| 코스피/코스닥/나스닥 지수 | Yahoo Finance 공개 API | 매일 09시 |
| 트렌드 리포트 (앱별 MAU/신규설치) | 구글 시트 연동 | 매일 09시 30분 |
| 커뮤니티 반응 (화제 키워드 + 감정분류 + 실제 반응) | 디시인사이드+에펨코리아 글 수집 + Claude 웹서치 | 매일 08시 |
| 구글 이미지 다운로드 실패분 복구 | `googleMediaBackfill.js` | 매일 07시 |
| 구글 이미지 소재 OCR 텍스트 백필 | `googleTextBackfill.js` (200건/일) | 매일 07시 30분 |
| 구글 광고 마지막 게재일 백필 (종료 처리된 광고만) | `googleLastShownBackfill.js` | 매일 07시 45분 |
| 날짜 정보 전혀 없는 구글 종료 소재에 2025년 추정치 채우기 | `noDateFallbackBackfill.js` | 매일 07시 50분 |

`ad-collector/scheduler.js`가 위 주기를 cron으로 관리하고, Windows 작업 스케줄러
(`AdCollectorScheduler`, 로그온 시 자동 시작)에 등록되어 있습니다. 서버로 옮겨도
"휴일이면 사람이 알아서 안 돌린다" 같은 개념이 없어서(크론은 휴일이든 아니든 정해진
시각에 그냥 실행됨), **주 1회짜리(메타/구글)만 놓치면 그 주 전체를 못 하게 되므로
공휴일 인지 로직을 넣어뒀습니다** (2026-08-05 추가):

- **메타/구글**: 크론 자체는 매일 오전 11시에 도는데, `scheduleUtils.js`의
  `isFirstBusinessDayOfWeek()`가 "이번 주(월요일 기준)에 처음 맞이하는 주말도 공휴일도
  아닌 평일"인지 확인해서 그날만 실제로 수집한다. 월요일이 공휴일이면 화요일로,
  추석처럼 월~수가 다 공휴일이면 목요일로 자동으로 밀린다. Apify는 호출당 비용이
  드는 유료 API라 이 로직이 있어도 주 1회 이상 늘어나지 않는다(공휴일 때문에 밀렸을
  뿐, 그 주 안에서 한 번만 돈다).
- **검색광고 일반키워드(파워링크)**: 이미 주 3회(월/수/금)라 한 번 놓쳐도 그 주 안에
  또 도니까, 그날이 공휴일이면 대체일 없이 그냥 건너뛴다(`isHoliday()`만 확인).
- **브랜드검색/검색광고 브랜드키워드**: 예전 방식(평일 13시, "이번 주에 이미 했는지"
  게이트) 그대로 유지 - 별도 공휴일 로직 없이도 크론이 그날 안 돌면 다음 평일에 자동
  으로 도는 구조라 그대로 둠.
- **공휴일 목록(`ad-collector/holidays.js`)**: 신정/삼일절/어린이날/현충일/광복절/
  개천절/한글날/크리스마스처럼 매년 같은 날짜인 것들은 확정값이고, **설날/추석/
  부처님오신날처럼 음력 기반이라 매년 날짜가 바뀌는 공휴일은 2026년 추정치**로만
  채워뒀습니다 - 실제 운영 전 공식 캘린더로 재확인하고, 매년 말에 다음 해 날짜를
  추가해줘야 합니다.
- 메타/구글이 이제 `collector.js`의 `runWeeklyMeta`/`runWeeklyGoogle`로, 파워링크
  일반키워드는 `runWeeklyPowerlink`로 각각 독립 함수로 분리되어 스케줄러가 직접
  호출합니다(`node collector.js --force-meta`처럼 수동 강제 실행 플래그는 그대로
  동작함).

## 데이터가 저장되는 방식

- 모든 수집 결과는 `ad-collector/data/*.json`에 저장됩니다 (광고 목록/인사이트/시세 등
  종류별로 파일이 나뉘어 있음 — 예: `index.json`, `bs_index.json`, `powerlink_index.json`,
  `powerlink_brand_index.json`, `community_trend.json` 등).
- 광고 이미지·영상·스크린샷 실물 파일은 `ad-collector/output/` 아래 저장됩니다
  (`images/meta/`, `images/google/`, `screenshots/` 등). 각 광고 데이터 항목의
  `localPath`/`localImage`/`screenshotPath` 필드가 이 상대경로를 가리킵니다.
- **S3 호환 스토리지 지원(선택)**: `ad-collector/.env`에 `S3_ENDPOINT`,
  `S3_REGION`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`,
  `S3_PUBLIC_BASE_URL` 6개 값을 채우면, 수집된 미디어를 로컬 대신 S3에 업로드하고
  `localPath` 등에 공개 URL을 그대로 저장합니다(업로드 성공 시 로컬 파일은 삭제).
  6개 중 하나라도 비어있으면 지금처럼 로컬 저장 그대로 동작합니다 (`.env.example` 참고).
- 각 광고 항목이 갖는 주요 텍스트 필드: `advertiserName`(광고주명), `headline`(제목),
  `copyText`(본문 카피), `keyword`(수집 시 사용한 검색어), `landingUrl`(랜딩 URL),
  `platform`(meta/google/naver_bs 등), `status`(active/ended), `collectedAt`(수집 시각).
  단, **구글 이미지형 광고는 원본(광고 투명성 센터)이 텍스트를 아예 제공하지 않아서
  `copyText`/`headline`이 항상 비어있습니다** — 아래 "소재 인사이트" 항목 참고.
- `node sync-data.js`(ad-ref 폴더 안에서 실행)가 위 JSON과 미디어 파일을
  `ad-ref/public/data/`로 복사합니다. S3를 쓰는 경우 미디어 파일 복사는 건너뜁니다
  (이미 공개 URL이라 로컬 사본이 필요 없음).

## 대시보드 탭별 기능

- **경쟁사 대시보드 - 전체 / 메타 / 구글 / 브랜드별**: `index.json`(Meta+Google 광고)을
  플랫폼/브랜드/기간으로 필터링해서 카드 그리드로 보여줍니다. 상단 KPI(활성/신규/종료/
  즐겨찾기)와 소재 인사이트(아래 참고)가 함께 표시됩니다.
- **브랜드검색**: 네이버 브랜드검색(PC/모바일) 스크린샷과, 그 안의 버튼을 실제로 클릭해서
  받아온 랜딩페이지 스크린샷을 브랜드별로 보여줍니다. 브랜드 소유 지면이라 소재 로테이션이
  파워링크보다 드물지만 A/B 로테이션이 관찰돼서, 한 번 수집할 때 페이지를 6회 새로고침하며
  "버튼 텍스트+영역 + 메인이미지 URL"을 합친 키로 신규 소재만 골라 누적합니다(문구가 같아도
  메인이미지만 바뀌면 별도 소재로 잡힘 - 텍스트 오버레이형뿐 아니라 모델/비주얼 이미지형
  로테이션도 이렇게 잡힙니다). PC/모바일 소재는 문구 유사도로 자동 매칭해서 같은
  `creativeSetId`(예: "소재1", "소재2")로 묶어 보여주며, 짝을 못 찾은 소재는 혼자만의
  세트로 남습니다.
- **검색광고 일반키워드**: "증권"/"주식" 두 키워드로 네이버 파워링크를 검색했을 때 뜨는
  광고 15개(제목/설명/서브링크/이미지형 서브링크/추가제목 포함)를 그대로 보여줍니다.
  이 탭은 **키워드 경매 특성상 우리 브랜드가 아닌 광고주도 섞여서 나오는 게 정상**입니다
  (경쟁 구도 파악용).
- **검색광고 브랜드키워드**: 9개 브랜드명 자체를 검색어로 파워링크를 검색하되, **그
  브랜드 본인이 낸 광고만** 걸러서 보여줍니다(`brandUtils.js`의 `matchesBrand`로 광고주명
  매칭 — 회사가 검색량 확보를 위해 자기 이름으로도 광고를 사는 경우가 많음). 한 번 수집할
  때 페이지를 30회 새로고침하며 그때그때 다르게 노출되는 소재를 누적해서 모으고, 30회를
  다 돌려도 하나도 안 잡히면 "광고 미집행 중"으로 표시합니다. 일부 브랜드는 검색 키워드와
  실제 광고주명이 달라서(예: NH투자증권의 MTS 앱 브랜드명이 "나무증권") `brandUtils.js`에
  별칭 매핑을 등록해둬야 합니다.
- **검색어 트렌드**: 네이버 데이터랩 검색량 추이를 브랜드별로 비교하고, 코스피/코스닥/
  나스닥 지수와 함께 보여줍니다. 조회 결과 아래에 그 추이에 대한 인사이트가 표시됩니다.
- **트렌드 리포트**: 팀이 관리하는 구글 시트(앱별 월간 MAU/신규설치 등)를 그대로 가져와
  표/그래프로 보여줍니다.
- **커뮤니티 반응**: "주식/투자 전반 화제 키워드"(시장 전체)와 "메리츠증권 화제 키워드"
  (브랜드 특정) 두 그룹을 매일 기록합니다. 고정 키워드 목록이 아니라 디시인사이드 주식
  갤러리(neostock)+에펨코리아 주식 게시판 글과 Claude 웹서치를 결합해서 그날그날 실제
  화제 키워드 TOP 15를 뽑고, 각각 감정 분류(긍정/중립/부정, 버블 색상)와 상대적 언급량
  (버블 크기)을 매깁니다. 캘린더로 과거 날짜를 골라볼 수 있고, TOP10 표에서 전일比·7일
  추이 스파크라인을 보여주며, 실제로 확인된 기사/커뮤니티 반응을 출처와 함께 보여줍니다.
  과거 이력은 이 기능 도입일(2026-08-05)부터 쌓이며, 그 이전 날짜는 디시인사이드 글
  작성일 기준으로 소급 백필한 것입니다(`backfillCommunityTrendHistory`).
- **데일리 뉴스 / 주간 인사이트**: 사이드바에 탭은 있지만 아직 구현 전(준비 중 placeholder)
  입니다.

## 인사이트는 어떻게 만들어지는가

세 종류(소재 인사이트/파워링크 인사이트/검색어 트렌드 인사이트/커뮤니티 반응 인사이트
포함, 총 여러 곳)가 있고, 전부 같은 규칙을 씁니다.

1. **호출 방식**: `ad-collector/insightClient.js`의 `generateInsight()`가
   `execFileSync('claude', ['-p', prompt, ...])`로 **로컬에 설치된 Claude Code CLI**를
   비대화형으로 호출합니다. 별도 Anthropic API 키가 아니라 이 컴퓨터에 로그인된 Claude
   Code 계정(구독)을 그대로 씁니다. `--allowedTools WebSearch`를 붙여서 웹 검색이 필요한
   프롬프트(커뮤니티 반응 화제 키워드 조사 등)도 처리할 수 있습니다 - Read(로컬 이미지
   읽기)는 기본 허용이지만 WebSearch는 명시적으로 허용해야 동작합니다. `generateInsight(prompt,
   data, {timeout})`로 호출별 타임아웃을 늘릴 수 있고(웹서치 포함 조사는 기본 3분→5분으로
   늘려둠), 이는 인터랙티브 세션과 사용량을 공유하므로 너무 자주/크게 호출하면 한도에
   영향 줄 수 있습니다.
2. **포맷 규칙**: 모든 인사이트 프롬프트는 `buildInsightPrompt()`로 감싸서, "첫 줄 = 한 문장
   요약, 그 아래 `- `로 시작하는 핵심 포인트 3~5개"로만 답하도록 강제합니다. 대시보드의
   `InsightBox` 컴포넌트가 이 포맷을 파싱해서 굵은 요약 + 불릿 리스트로 렌더링합니다.
   분석할 소재가 부족하면 다른 말 없이 정확히 "인사이트 없음"이라고만 답하게 되어 있고,
   호출 전에 소재 개수가 3개 미만이면 아예 Claude를 호출하지 않고 바로 그 문구로 저장합니다
   (비용 절감).
3. **생성 시점 - 두 가지 방식이 섞여 있음**:
   - **수집 시점에 미리 생성 → JSON에 저장** (파워링크 일반/브랜드키워드 인사이트,
     커뮤니티 반응 인사이트): `collector.js`/`scheduler.js`가 수집 직후 자동으로 호출하고,
     결과 텍스트를 `data/*.json`에 저장해둡니다. 대시보드는 이 저장된 텍스트를 그냥
     보여주기만 합니다.
   - **화면에서 버튼을 눌러 그때그때 생성** (경쟁사 대시보드 '전체' 탭의 소재 인사이트,
     검색어 트렌드 인사이트): 사용자가 지금 화면에 필터링된 조건(브랜드/기간/매체 등)
     그대로 보내면, `ad-ref/src/app/api/creative-insight/route.ts` 같은 API 라우트가
     그 조건에 맞는 데이터만 다시 샘플링해서 매번 새로 생성합니다. 이 라우트가 정적
     export로 안 되고 실제 Node 서버가 필요한 유일한 부분입니다.
4. **텍스트가 없는 소재(주로 구글 이미지 광고)는 이미지를 직접 읽게 함**: 구글 광고
   투명성 센터는 이미지형 소재에 카피 텍스트를 전혀 안 주기 때문에, `copyText`/`headline`이
   비어있으면서 이미지가 있는 항목은 로컬 이미지 파일 경로를 프롬프트에 같이 넘겨서 Claude가
   직접 열어보고 읽게 합니다. 다만 **한 번 호출에 최대 6장까지만** 보내기 때문에, 소재가
   수백~수천 건인 브랜드는 매번 극히 일부만 보고 인사이트를 만드는 한계가 있습니다
   (근본 해결은 수집 시점에 이미지 텍스트를 한 번 OCR로 뽑아 `copyText`에 영구 저장해두는
   것 — 아직 미구현, 필요시 요청해주세요).

⚠️ **서버 운영 시 주의**: `execFileSync('claude', ...)` 방식은 이 컴퓨터에 로그인된
개인 Claude Code 세션을 그대로 쓰는 것이라, 로그인 세션이 없는 서버에서는 그대로 동작하지
않습니다. 서버 배포 방법은 별도로 안내드립니다.

---

## 최근 진행 상황 (2026-08-09 기준)

이전 핸드오프(2026-08-04) 이후 진행된 내용과 아직 안 끝난 것들입니다.

### 완료된 것
- **S3 스토리지 업로드** (`ad-collector/storage.js`) - 위 "데이터가 저장되는 방식" 참고
- **다크/라이트 테마 토글** - `ad-ref/src/app/globals.css`에 CSS 변수로 색상 정의,
  `ThemeProvider.tsx`/`ThemeToggle.tsx`로 전환(사이드바 상단 버튼), 라이트 모드는 메리츠
  CI 빨강 계열(`#e6483f`)을 포인트 컬러로 사용. 컴포넌트 전반의 하드코딩 hex 색상을 변수로
  교체했으나 완벽히 100%는 아닐 수 있음 - 이상한 색이 보이면 해당 컴포넌트에 남은 하드코딩
  hex를 찾아 `var(--...)`로 바꾸면 됨.
- **구글 이미지 소재 OCR 백필** (`googleTextBackfill.js`) - 구글 이미지형 광고는 원본이
  카피 텍스트를 안 줘서, 로컬 이미지를 Claude가 직접 읽어 `copyText`에 채워 넣음. 매일
  200건씩 자동 처리(scheduler.js). 그 전 단계로 원래 다운로드 자체가 실패해서
  `localPath`가 비어있던 항목(1,092건, 주로 메리츠 - 원인은 최초 수집 시 레이트리밋
  추정)을 복구하는 `googleMediaBackfill.js`도 매일 자동 재시도됨.
- **커뮤니티 반응 탭 전면 개편** - 위 "대시보드 탭별 기능" 참고. 고정 키워드 → 실시간 화제
  키워드, 감정분류, 캘린더, TOP10+스파크라인, 실제 반응 인용까지 추가됨.
- **⚠️ Meta 소재 이미지/문구 불일치 핵심 버그 수정** - `metaAdDetail.js`가 개별 광고 이미지를
  가져올 때 쓰던 `facebook.com/ads/library/?id=...` 페이지가 실제로는 광고 1건이 아니라
  ~16개 결과 목록을 보여줘서, 캐러셀이 아닌 완전히 다른 광고의 이미지가 섞여 저장되는
  버그가 있었음(저장된 39건 중 30건, 77%가 잘못된 이미지였음 - 확인 후 전부 재검증/수정
  완료). 페이지에 표시되는 "라이브러리 ID: {id}" 텍스트로 정확한 광고 범위를 찾는 방식으로
  수정. `meta.js`(키워드 스크래퍼)의 adId/원본링크 추출도 페이스북 DOM 변경으로 죽어있던 걸
  같은 방식으로 수정.

### 2026-08-05 세션(2차)에서 추가로 완료된 것
- **✅ Meta 키워드 스크래퍼(`meta.js`) 광고주명 추출 수정** - 원인은 카드 경계(`cardEls`)
  탐지 자체였음: 예전 셀렉터(`._7jyg` 등)가 지금 DOM에 전혀 안 맞고, `preWrap` 6단계
  위 조상 폴백은 범위가 너무 좁아 광고주명 앵커(상단 프로필 링크)까지 못 미쳤음. adId
  추출에 이미 쓰던 "라이브러리 ID:" 텍스트 마커 기반 스코핑(정확히 광고 1건으로 스코프된
  가장 안쪽 조상 찾기)을 카드 경계 탐지 자체로 승격시켜 해결 - 실측으로 검증(삼성증권
  검색 시 실제 광고 18건이 모두 정확한 광고주명으로 추출됨, 비금융 광고주 12건은 정상
  필터링). 같은 조사 중 발견한 부수 버그도 같이 수정: 실제 광고가 0건인 브랜드(예:
  메리츠증권, 검색 결과 "검색 기준과 일치하는 광고가 없습니다")에서 네트워크 인터셉트
  폴백이 페이지 잡음(내비게이션 아이콘 등)에 브랜드명을 그대로 붙여 가짜 광고 항목으로
  저장하던 문제 - 실제 카드가 0개일 때는 이 폴백을 아예 돌리지 않도록 수정.
- **✅ 구글 광고 마지막 게재일(`adLastShownAt`) 백필 추가** - 확인해보니 `google.js`
  자체엔 이미 파싱 로직이 있었음(2026-08-04 커밋에 추가돼있었고 정상 동작 중 - 이전
  버전의 이 문서 설명이 stale했음). 실제 이슈는 수집 시점에 아직 게재 중이던 광고는
  이 필드가 원본에 아예 없어서(종료된 광고만 표시) null로 남고, 이후 `tracker.js`가
  `status: 'ended'`로 넘겨도 상세페이지를 재방문하지 않아 영영 못 채우는 것이었음(실측
  - ended 43건 중 26건이 비어있었음). `googleLastShownBackfill.js`를 추가해 종료 처리된
  뒤 상세페이지를 다시 방문해 채우도록 함(매일 07:45 자동 실행, `scheduler.js`에 등록).
  기존 index.json에 밀려있던 대상 11건은 즉시 백필 완료(나머지 15건은 애초에
  `detailsLink` 자체가 없는 더 예전 수집분이라 재방문 불가 - 별개 문제).

- **✅ 브랜드별 Meta 광고 미수집 문제 해결 - 비용 발생 없이 확인됨** - 삼성증권 기준 실제
  게재 중 17개 vs 저장 5개 문제의 근본 원인은 Apify가 아니었음. 코드 확인 결과
  `apifyMetaClient.js`의 `matchesBrand` 필터는 `type==='brand'`일 때만 쓰이는데, 실제
  Apify 호출부(`account1.js`/`account2.js`)는 둘 다 `type:'keyword'`("증권"/"주식" 일반
  검색)로만 호출해서 **`matchesBrand`는 실제로는 어디서도 호출되지 않는 죽은 코드**였음.
  브랜드별 정확한 수집은 원래 `metaBrandBatch.js`(2시간마다 자동 순환)가 담당하는데, 이게
  Apify가 아니라 위에서 고친 `scrapers/meta.js`를 직접 쓰고 있었고, 바로 그 광고주명
  추출 버그 때문에 거의 다 걸러지고 있었던 것. 라이브 Apify 호출(비용 발생) 없이
  `metaBrandBatch.js`를 실제로 한 번 실행해 검증 - 삼성증권 저장분이 5개 → 17개(오늘
  신규 활성 12개 + 기존/종료 5개)로 정확히 일치함을 확인함.

- **✅ Meta 이미지/카피 불일치 - 전체 브랜드 재검증** - 토스증권 광고 카드에 카피와 전혀
  무관한 산/구름 스톡사진이 붙어있다는 제보로 조사. `metaImageVerify.js`를 추가해 adId
  있는 기존 이미지 56건 전부 상세페이지 재방문으로 대조(50건 일치, 6건은 광고가 오래돼
  Facebook이 더 이상 상세정보를 안 줘서 자동 대조 불가 - 이 6건은 직접 이미지를 열어
  육안 확인: 대신증권 2건·나무증권 1건은 정상, 토스증권 3건은 전부 동일한 무관 스톡사진으로
  확인돼 삭제). adId 없이 브랜드명만 붙은 "네트워크 인터셉트 폴백" 항목 15건(60x60 프로필
  아이콘 등 잡음으로 확인됨)도 함께 정리 - 이 폴백 자체를 `meta.js`에서 완전히 제거함
  (실제 광고 소재는 마커 기반 카드 스코핑만으로 충분히 잡히는 게 확인됐고, 폴백은 adId/
  카피 등 아무 메타데이터도 못 붙여서 검증 불가능한 잡음만 추가했음). 이후 9개 브랜드
  전체를 고쳐진 스크래퍼로 재수집해 토스증권 등 최신 소재로 교체 완료.

### 2026-08-05 세션(3차)에서 추가로 완료된 것
- **✅ `ad-ref` 대시보드 UI 전면 개편** - 아이콘 기반 사이드바 내비게이션, 그림자 기반
  카드 호버, 정리된 2열 상세 모달로 기본 UI를 통째로 교체(예전 UI는 삭제 - 되돌릴 필요
  생기면 git 이력에서 복원 가능). 커뮤니티 반응 버블차트는 라이트 모드에서 글씨가
  안 보이던 문제(불투명도 고정값 문제), 작은 원에서 폰트가 너무 작던 문제, 단어 중간이
  잘리던 줄바꿈 문제를 모두 고쳤고(그리디 워드랩으로 교체), 원 안 언급량 숫자 표시는
  제거함. 커뮤니티 반응에 "기간(범위)" 선택 모드를 추가해 여러 날짜 언급량을 합산해서
  볼 수 있게 함. 경쟁사 대시보드 메타/구글 탭에 광고주 슬라이서 추가(데이터 기반 자동
  생성이라 새 광고주가 잡히면 자동으로 나타남).
- **✅ 수집 스케줄 개편 + 공휴일 인지 로직 추가** - 메타(Apify+키워드)/구글을 기존
  `collect()`의 평일 13시 게이트에서 분리해 `runWeeklyMeta`/`runWeeklyGoogle`
  독립 함수로 만들고, 매일 오전 11시에 "이번 주 첫 평일(공휴일 제외)"인지 확인해서
  그 날에만 실행되도록 변경(`scheduleUtils.js`). 검색광고 일반키워드(파워링크)는
  `runWeeklyPowerlink`로 분리해 월/수/금 오전 11시(공휴일이면 그날만 스킵)로 변경.
  브랜드검색/검색광고 브랜드키워드는 그대로 `collect()`에 남겨 기존 방식(평일 13시)
  유지. 공휴일 목록(`holidays.js`)의 음력 기반 공휴일(설날/추석/부처님오신날)은 2026년
  추정치이니 운영 전 재확인 필요 - 위 "수집 대상 및 주기" 섹션 참고.

### 2026-08-06~08-08 세션에서 추가로 완료된 것
- **✅ 브랜드검색 A/B 로테이션 감지 + PC/MO 세트 매칭 추가** - 위 "대시보드 탭별 기능 >
  브랜드검색" 참고. 문구뿐 아니라 메인이미지(모델/비주얼형 포함)가 바뀌는 로테이션도
  잡아내고, PC/모바일 소재를 자동으로 같은 세트로 묶어 보여줌.
- **✅ 구글 9개 브랜드 전체 백필 완료 + 스크롤/멀티계정 버그 다수 수정** - 무한 스크롤 상한을
  30회 → 500회로 늘리고 "3번 연속 높이 안 늘어남"으로 종료 판정을 바꿔 삼성증권처럼
  2,000건 넘는 광고주가 중간에 잘리던 문제를 해결. 한 브랜드가 구글에 여러 광고주 계정으로
  나뉜 경우(미래에셋증권 등) 두 번째 계정부터 조용히 스킵되던 버그를 후보별 완전히 새
  브라우저 컨텍스트로 바꿔 해결(같은 세션으로 상세페이지를 수백 개 방문하면 구글이 재검색
  자체를 빈 목록으로 응답하는 현상 확인). NH투자증권·KB증권은 구글 투명성 센터에 직접
  검색해서 "결과 0개"를 확인함 - 버그가 아니라 실제로 구글 광고를 안 하거나 다른 광고주명을
  쓰는 것으로 보임.
- **✅ 구글 pHash 오탐 중복제거 버그 수정** - 구글 이미지는 이미 URL 자체가 콘텐츠 기반(CDN
  ID)이라 URL 중복판정만으로 충분한데, 여기에 시각적 유사도(pHash) 중복판정까지 얹어서
  같은 템플릿/모델사진에 문구만 다른 소재를 대량으로 "중복"이라며 걸러내고 있었음(실측:
  한 세션에서 2,246건 중 544건, 24% 오탈락 - 사실상 신규 수집이 거의 안 되는 것처럼 보였던
  근본 원인). 구글은 pHash 판정 자체를 건너뛰도록 수정 - 메리츠증권 기준 1,597 → 2,141건
  등 전반적으로 저장량이 크게 늘어남.
- **✅ 8개 이상 스크립트의 동시쓰기(레이스 컨디션) 데이터 손실 버그 전면 수정** - 여러
  백필/수집 스크립트가 각자 시작 시점에 `index.json`을 한 번만 읽어 메모리에 들고 있다가
  (이미지 다운로드 등으로 몇 분~몇 시간씩 걸리는 루프 동안), 그 사이 다른 스크립트가 같은
  파일에 써놓은 내용을 감지 못 하고 저장 시점에 자기 스냅샷으로 덮어써버리는 문제(실측:
  267건 신규 수집분이 이 방식으로 한 번 사라짐). 저장 시점마다 파일을 다시 읽어서 자기가
  책임지는 필드만 id 기준으로 병합하는 패턴으로 전체 교체(`processItems.js`,
  `googleLastShownBackfill.js`, `googleMediaBackfill.js`, `googleTextBackfill.js`,
  `metaImageVerify.js`, `scrapers/metaAdDetail.js`, `scrapers/powerlinkBrandKeyword.js`,
  `collector.js` 등).
- **✅ 구글 이미지 소재 설명(`aiDescription`)/시각적 유사도(`visualHash`) 백필 추가** -
  문구가 없는 모델·비주얼형 구글 이미지 소재에 Claude가 이미지를 직접 보고 "문구뿐 아니라
  모델/상황/분위기까지" 설명한 텍스트를 채워 넣음(`googleDescriptionBackfill.js`, 459건
  완료) - 소재 인사이트가 이런 소재도 반영하게 됨. 화면 표시용 "비슷한 소재 접기" 토글에
  쓰는 `visualHash`도 기존 다운로드된 이미지 기준으로 일괄 채움(`googleVisualHashBackfill.js`,
  2,618건 완료).
- **✅ 대시보드 카드 개수 제한/정렬/중복표시 개선** - 전체/메타/구글 탭은 최근 150개만
  그리던 게 브랜드 상세 화면에도 잘못 적용돼 수천 건 수집된 브랜드가 280개 정도만 보이는
  것처럼 보이던 문제를 브랜드 상세는 전체 표시(무제한)로 분리. 문구가 같아도 URL이 다른
  걸 보고 "중복 수집됐나?" 오해하기 쉬운 문제를 위해 화면 표시만 접는(데이터는 그대로 두는)
  "비슷한 소재 접기" 토글 추가. 전체/브랜드별/메타/구글 4곳에 "최신순 / 게재일 긴 순서"
  정렬 토글을 추가함.
- **✅ "날짜 없는 소재가 최신순 맨 위로 뜨는" 정렬 버그 수정 + 2025년 추정치 채움** - 구글
  종료 소재 중 상세페이지가 더 이상 마지막 게재일을 안 보여주는 오래된 소재(전체의 약
  48%, 2,121건)는 날짜 계산이 "수집일"로 대체됐는데, 최근 백필로 재발견된 시점이 최근
  날짜라 "최신순"에서 실제로는 오래된 소재가 맨 위로 뜨는 문제가 있었음(메리츠증권 모델
  이미지 소재로 실측 확인). 정렬 로직을 "실제 날짜 있는 소재 우선"으로 고치고, 더 이상
  구할 수 없는 실제 날짜 대신 2025-01-01을 추정치로 채워 넣음(`adDateEstimated` 플래그로
  확정값과 구분, `noDateFallbackBackfill.js`, 매일 07:50 자동 실행 - 위 "수집 대상 및
  주기" 참고). 같은 현상의 메타 소재 5건은 광고 본문상 실제로는 2026년 소재로 확인돼
  2025로 덮지 않고 제외함(원인이 다른 별개 버그로 추정).
- **✅ 커뮤니티 반응 날짜별 소급 백필 기능 추가** - 스케줄러가 며칠 안 돌아 08-04/06/07이
  빠졌던 걸, 디시인사이드 글 목록의 실제 작성일(페이지를 여러 장 넘기며 날짜별로 모음)
  기준으로 소급 조사하는 `backfillCommunityTrendHistory(settings, daysBack)` 추가 후
  실제로 3일치 백필함.
- **✅ Vercel 데모 배포 문서화** - 위 "3. 서버 배포 방법" 참고 (임시 조치/한계 포함).

### 2026-08-09 세션에서 추가로 완료된 것
- **✅ "미래에셋증권 주식회사" 계정 500건 - 문구에 광고주명이 잘못 채워지던 버그 수정** -
  구글 카드 파싱 시 "길이 10~400자인 첫 텍스트"를 문구로 잡는 로직이, 이 계정 카드에서는
  광고주명 라벨을 실제 카피보다 먼저 만나서 `copyText`에 "미래에셋증권 주식회사"만 채워
  넣고 있었음(대시보드에 문구로 노출됨 + "문구 있음"으로 오인돼 디스크립션 백필도 건너뜀
  + 소재 인사이트에서 중복제거하면 전부 같은 값이라 사실상 데이터가 1건으로 뭉쳐져
  "분석할 소재 부족"으로 나오는 문제까지 연결됨). `scrapers/google.js`에서 후보 텍스트가
  광고주명과 같으면 무시하도록 수정하고, 기존 500건의 잘못된 `copyText`는 비워서 정상적인
  "문구 없음" 상태로 되돌림(재수집 없이 데이터만 정정 - 이후 자동으로 디스크립션 백필
  대상이 됨).
- **✅ 구글 이미지 소재 디스크립션(`aiDescription`) 백필 - 메리츠증권 외 8개 브랜드 전체로
  확장** - 처음엔 메리츠증권만 대상으로 수동 실행했었는데, 그때는 나머지 8개 브랜드의 구글
  백필이 아직 안 끝나서 후보 자체가 없었을 뿐 브랜드로 범위를 좁혀둔 코드는 아니었음 -
  전체 브랜드 백필이 끝난 지금 다시 돌려보니 1,042건이 새로 대상에 잡힘(위 문구 버그로
  풀려난 미래에셋증권 500건 포함). 매일 300건씩 자동으로 채워지도록 스케줄에도 등록
  (07:35, `googleDescriptionBackfill.js`). **실행 결과(2026-08-09): 291건 성공, 751건은
  "org's monthly spend limit" 에러로 실패**(아래 항목 참고) - 실패분은 `aiDescription`이
  그대로 비어있어서 계속 대상 조건에 걸리니, 한도 문제만 풀리면(계정 전환 등) 스케줄이든
  수동 실행이든 자동으로 이어서 채워짐. 751건을 빨리 채우고 싶으면 300건/일 스케줄로는
  3일 걸리니 계정 전환 후 `node googleDescriptionBackfill.js`로 한 번에 수동 실행 권장.
- **⚠️ 발견: `ad-collector`가 쓰는 `claude` CLI 로그인 계정이 팀원 개인 계정으로 돼있어서
  그 계정의 조직 월 지출 한도에 걸림** - 위 디스크립션 백필이 도중에 "You've hit your
  org's monthly spend limit"로 실패한 원인. 이 대화(Claude Code 세션)의 팀 플랜 사용량
  (5시간/주간 한도)과는 완전히 별개의 한도임 - Anthropic Console API 조직 단위 월 $ 지출
  한도로 보임. 재테스트해보니 한도 자체는 이미 풀린 상태였음(일시적이었던 것으로 추정).
  **팀원 개인 계정에 의존하는 지금 구조의 실제 리스크가 처음으로 발생한 사례**이니, 다음
  세션부터는 이 컴퓨터의 `claude` CLI를 본인 계정으로 재로그인해서 진행하기로 함(위
  "2. Claude CLI 기반 인사이트" 섹션에 적어둔 리스크가 그대로 발생한 것 - 계정 소유자가
  본인 작업에 쓰려 할 때 한도가 이미 소진돼 있을 수 있다는 그 우려).

**업데이트**: 위 한도는 재테스트 시점에 이미 풀려있었음이 확인됨. 남은 751건 디스크립션
백필을 이 계정 그대로 다시 실행 중(사용자가 잠든 사이 백그라운드로 진행) - 결과는 아래
"전체 점검" 항목에 이어서 기록.

### 2026-08-09 심야 - 대시보드 전체 점검 (뉴스클리핑 제외)
사용자가 자는 동안 대시보드 전체(데일리뉴스/주간인사이트 placeholder 제외)를 데이터
무결성 + 콘솔 에러 + 스크래핑 구조 관점에서 점검함. 발견/수정한 것:
- **✅ 레거시 메타 소재 36건에 `id` 필드 누락 - React "중복 key" 경고의 원인이었음** -
  `id`가 없어서 카드 목록에서 `key={item.id}`가 전부 `undefined`로 겹쳤음. 전부
  2026-08-04 이전 데이터로, 그 이후 수집분은 전부 정상(현재 `scrapers/meta.js`는 항상
  `id`를 채움 - 예전 코드 경로의 흔적으로 추정). `adId` 기반으로 안전하게 `id`를
  채워 넣어서 해결 - sync 후 실제로 해당 콘솔 경고가 사라지는 것까지 확인함.
- **✅ `marketIndex.js` - 지수 하나만 조회 실패해도 그 지수의 기존 1년치 데이터 전체가
  파일에서 사라지는 버그** - 코스피/코스닥/나스닥 중 하나가 그날 일시적으로 조회
  실패하면, 실패한 키가 결과 객체에 아예 안 담긴 채로 파일 전체를 덮어써서 그 지수의
  기존 캐시가 통째로 삭제됐었음. 이제 실패한 지수는 기존 값을 그대로 유지하도록 수정.
- **✅ `trendReport.js` - 구글 시트 파싱이 0행이 되면(시트 구조 변경 등) 기존 데이터를
  그대로 덮어써서 날려버리는 버그** - `market_index.js`를 고치며 같은 유형의 버그를
  찾아서 같이 수정. 0행이면 저장을 건너뛰고 기존 파일을 유지하도록 함.
- **점검 결과 문제 없음으로 확인된 것들**: `index.json` 4,459건 전수 - id 중복 0건,
  platform 누락 0건, 미디어 URL 둘 다 없는 항목 0건, 로컬 이미지 파일 샘플 500건 전부
  실존 확인. 인사이트 4곳(소재/파워링크 일반·브랜드/검색어 트렌드) 전부 "소재 3개
  미만이면 호출 자체를 안 하고 정확히 '인사이트 없음'으로 저장"하는 공통 로직
  (`insightClient.js`의 `hasEnoughDataForInsight`)을 일관되게 쓰고 있음을 코드로 확인.
  트렌드 리포트/커뮤니티 반응 탭 모두 실제 데이터로 정상 렌더링, 콘솔 에러 없음(단
  하이드레이션 불일치 경고는 라이트/다크 테마가 마운트 후에 적용되는 흔한 패턴이라
  기존부터 있던 것이고 기능엔 영향 없음 - 필요시 다음에 손볼 것으로 남겨둠).
  커뮤니티 반응 버블차트에서 "중복 key" 경고가 한 번 더 관찰됐으나, 실제 데이터
  (전 날짜×그룹 전수조사)와 화면에 그려진 DOM 둘 다에서 중복 키워드를 못 찾아서
  재현 가능한 원인은 못 찾음 - 초기 렌더링 시점의 일시적 현상으로 추정, 낮은 우선순위로
  남겨둠.
- **디스크립션 백필 751건 재실행 - 결과: 517건 성공, 234건 다시 같은 한도 에러로 실패**
  (사용자가 자는 동안 백그라운드 진행, 완료 후 sync-data.js + git push까지 마침). 같은
  계정으로 재실행했는데 도중에 또 "org's monthly spend limit"에 걸린 걸 보면, 이 한도는
  "한 번 걸리면 몇 분 뒤 완전히 풀리는" 단순한 일시적 상태가 아니라 **그 계정에 남아있는
  잔여 예산이 얼마 안 돼서, 어느 정도 쓰면 다시 바로 걸리는 구조**로 보임 - 계정 전환이
  1회성 임시조치가 아니라 실제로 필요해 보임. 최종 집계: 전체 대상 1,042건 중 808건
  (291+517) 채워짐, 234건 남음(전부 여전히 `aiDescription` 비어있어서 계속 대상 조건에
  걸림 - 매일 07:35 스케줄 또는 계정 전환 후 수동 실행으로 자동 이어짐).
- **✅ 디스크립션 백필 완료 - 남은 234건도 세 번째 실행에서 227건 성공(실패 7건은 전부
  타임아웃/깨진 이미지, 한도 에러 없었음)**. 최종 집계: 전체 대상 1,042건 중 1,035건
  처리 완료(`aiDescription` 1,488건 채워짐 - 이전 세션분 포함, `brokenImage`로 표시된
  7건 제외), 남은 6건은 타임아웃이라 다음 스케줄에서 자동 재시도됨. 한도 문제는
  결국 두 번째 실행에서만 발생했고 세 번째는 안 걸림 - 계정 잔여 예산이 그때그때
  달랐던 것으로 보임(정확한 원인은 불명, 계정 전환이 여전히 권장됨).

### 아직 안 끝난 것 (다음 세션에서 이어서 할 것)
0. **`claude` CLI 로그인 계정을 팀원 개인 계정 → 본인 계정으로 전환** - 위 "⚠️ 발견"
   항목 참고(두 번째 재실행에서도 같은 한도에 다시 걸린 걸 보면 임시조치가 아니라 실제
   전환이 필요해 보임). 전환 후 남은 디스크립션 백필 234건 수동 실행 권장(`node
   googleDescriptionBackfill.js`, ad-collector 폴더에서).
1. **`googleTextBackfill.js`(OCR)와 `googleDescriptionBackfill.js`(aiDescription) 필드
   겹침 정리** - OCR이 이미지 안 문구를 먼저 `copyText`에 "텍스트없음"으로 채우면
   `googleDescriptionBackfill.js`의 대상 필터(`copyText` 비어있음)가 그 소재를 건너뛰게
   됨 - 두 백필의 역할을 정리(문구 있으면 OCR만, 없으면 aiDescription까지)하거나 하나로
   합쳐야 함. 발견만 해두고 아직 손 안 댐.
2. **다른 광고주 계정에도 "문구=광고주명" 버그가 있는지 전체 재확인** - 이번엔 미래에셋증권
   주식회사 계정에서만 발견됐지만(전수 조사로 이 패턴만 확인함), 카드 DOM 구조가 광고주마다
   달라서 다른 계정에서도 비슷한 다른 오탐(예: 버튼 라벨, 안내문구 등을 카피로 오인)이 있을
   가능성은 배제 못 함 - 육안 확인 위주로 더 봐야 함.
3. **Vercel 데모의 Claude 인사이트 API 라우트 동작 여부 확인** - 위 "3. 서버 배포 방법"의
   ⚠️ 항목 참고. 로컬 `claude` CLI 세션에 의존하는 방식이라 Vercel 서버리스 환경에서
   그대로 동작할지 미확인 - 안 되면 최소한 에러 문구라도 명확하게 나오는지 확인 필요.
4. **서버 인증(claude setup-token)** - 실제 상시 운영 서버(로컬 PC가 아닌 곳)가 정해지면
   `claude setup-token`으로 발급받은 토큰을 그 서버 환경변수(`CLAUDE_CODE_OAUTH_TOKEN`)로
   넣어주면 코드 변경 없이 지금 방식 그대로 동작함(팀 공용 계정으로 발급 권장). 지금은
   팀원 개인 Claude 계정 로그인 세션 그대로 씀(로컬 PC/스케줄러 기준).

---

## 운영 관련 확인/결정 필요 사항 (2026-08-04 기준)

로컬 1인 사용을 넘어 팀 서버로 옮기기 전에 아래 항목들이 정리되어야 합니다.

### 1. 수집된 미디어(브검/구글 소재 등) 저장 위치
~~현재 팀원 개인 PC에만 로컬 저장 중~~ → **S3 호환 스토리지 업로드 기능을 추가해뒀습니다**
(`ad-collector/storage.js`). `ad-collector/.env`에 실제 S3 접속 정보 6개
(`S3_ENDPOINT`/`S3_REGION`/`S3_BUCKET`/`S3_ACCESS_KEY_ID`/`S3_SECRET_ACCESS_KEY`/
`S3_PUBLIC_BASE_URL`, `.env.example` 참고)만 채워주시면 이후 수집분부터 자동으로
S3에 업로드되고 로컬에는 안 쌓입니다. 실제 스토리지 계정/버킷 발급만 확인해주시면 됩니다.

### 2. Claude CLI 기반 인사이트 — 서버 운영 시 전환 필요
지금은 로컬 PC에 로그인된 개인 Claude Code 계정으로 CLI를 호출해 인사이트를
생성합니다. 이 방식은 **개인용 대화형 사용을 전제로 한 것**이라 여러 사람이
쓰는 서버에 그대로 올리기엔 적합하지 않습니다 (인증 세션을 서버에서 상시
유지해야 하는 문제, 플랜 사용량을 여러 요청이 공유하는 문제 등).

**✅ 결정 (2026-08-05)**: 현재 팀에 Claude Team/Enterprise 계정이 없고 전원
개인 계정으로 쓰고 있는 상황이라, 우선은 **팀원 개인 계정으로 발급받은
`claude setup-token` 토큰**을 서버 환경변수(`CLAUDE_CODE_OAUTH_TOKEN`)에 넣어
그대로 운영하기로 함. 나중에 팀/엔터프라이즈 계정이 생기면 그 계정으로 다시
`claude setup-token`을 발급받아 이 환경변수 값만 교체하면 되고, 코드 변경은
필요 없음.

- **왜 이렇게 결정했는지**: 팀 계정이 생길 때까지 서버 배포 자체를 미룰 이유가
  없다고 판단함 - 지금 규모(팀 내부 소규모 도구)에서는 개인 토큰으로 시작하는
  실용적 선택.
- **알아둬야 할 리스크(토큰 발급자가 바뀌기 전까지 유효)**:
  - 토큰을 발급한 그 개인이 퇴사/부서이동하거나 비밀번호·2FA를 바꾸면 서버의
    인사이트 기능이 예고 없이 멈출 수 있음 - 원인 파악이 늦어질 수 있으니 이
    README를 참고해서 어느 계정 토큰인지 확인할 것.
  - 서버 트래픽(여러 팀원이 "지금 조건으로 인사이트 생성" 버튼을 누르는 것 등)이
    전부 그 한 사람의 **개인 사용 한도**를 갉아먹음 - 그 사람이 정작 본인 작업에
    Claude Code를 쓰려 할 때 한도가 이미 소진돼 막힐 수 있음.
  - 개인용 플랜(Pro/Max 등) 약관이 이런 서버/자동화 용도 사용을 제한하는지는
    별도 확인 필요.
- 실제 서버 배포 시 누구 계정으로 토큰을 발급했는지 이 섹션에 기록해둘 것.

### 3. 서버 배포 방법
**✅ 임시 데모용 배포 완료 (2026-08-05, Vercel)**: 팀 내부 피드백용으로 `ad-ref`를 Vercel에
올려뒀습니다. `output: 'export'`(정적)와 API 라우트 3개(서버리스 함수)가 같은 빌드에서 같이
동작하는 게 Vercel 런타임과 잘 맞아서 별도 서버 없이도 가능했습니다. 다만 아래 두 가지는
임시 조치/한계이니 실제 운영 배포 전에 정리가 필요합니다:
- **데이터가 실시간이 아님**: 평소엔 `ad-ref/public/data/*.json`을 gitignore하는데, Vercel이
  git 저장소를 그대로 빌드하는 구조라 데모에 보여줄 데이터가 있어야 해서 예외적으로 커밋해둔
  상태입니다(`ad-ref/.gitignore` 참고, S3 연동 전까지의 임시 조치). 즉 **로컬에서 수집이
  계속 새로 되어도, 누군가 `git push`를 해야만 Vercel 데모에 반영**됩니다 - 자동 동기화가
  아닙니다.
- **이미지/스크린샷 실물 파일은 안 올라감**: `ad-ref/.vercelignore`가 `public/data/images/`,
  `public/data/screenshots/`를 제외합니다(15,000여 개 파일, 700MB+ - git/Vercel에 올리기엔
  너무 큼). 카드에 이미지 대신 플레이스홀더 아이콘만 보입니다.
- **⚠️ Claude 인사이트 API 라우트는 Vercel에서 아마 동작 안 함 (미확인)**: 세 API 라우트
  (`creative-insight`, `trend-insight`, 파워링크 인사이트)는 로컬에 설치·로그인된 `claude`
  CLI를 그대로 호출하는 방식인데(위 "인사이트는 어떻게 만들어지는가" 참고), Vercel 서버리스
  함수 환경에는 `claude` 바이너리도 없고 로그인 세션도 없습니다. `CLAUDE_CODE_OAUTH_TOKEN`
  환경변수도 Vercel 프로젝트 설정에 아직 등록 안 돼 있습니다 - 데모에서 인사이트 버튼을
  누르면 에러가 날 가능성이 높습니다. 실제 확인 및 조치는 다음 세션 과제로 남겨둡니다.

**아직 정해지지 않은 것 (실제 운영 배포 시 확인/결정 필요)**:
- `ad-collector`(수집기)는 Vercel과 무관하게 계속 특정 컴퓨터(또는 서버)에서 스케줄러로
  상시 실행되어야 함(현재는 팀원 개인 PC의 Windows 작업 스케줄러로 상시 실행 중).
- `sync-data.js`는 자동 실행되지 않으므로, 수집 후 자동으로 Vercel 데이터가 갱신되게
  하려면 "수집 → sync-data.js → git commit/push" 전체를 자동화하는 별도 작업이 필요함.

---

## 폴더별 상세 안내

- 수집기 설치/실행 방법: [`ad-collector/README.md`](ad-collector/README.md)
- 대시보드 개발 서버 실행: `ad-ref/` 에서 `npm run dev`
- 설정 파일 예시: [`ad-collector/settings.example.json`](ad-collector/settings.example.json),
  [`ad-collector/.env.example`](ad-collector/.env.example) (둘 다 실제 값을 채운 뒤 각각
  `settings.json`/`.env`로 파일명을 바꿔서 사용 — 두 파일 모두 git에는 올라가지 않습니다)

---

## 현재 시스템 구성 한눈에 보기 (2026-08-09 기준)

위 섹션들에 흩어져 있는 내용을 한 번에 훑어볼 수 있도록 홈페이지 구성/스크래핑 방식/탭별
기능을 여기 한 번 더 정리합니다(세부 내용은 각 섹션 링크 참고).

### 대시보드(사이드바) 구성
```
Wisebirds 경쟁사 모니터링          ← 로고, 클릭하면 홈(수집 현황)으로
[라이트 모드] [다크 모드]           ← 테마 토글

뉴스 클리핑
  📰 데일리 뉴스                   ← 준비 중 (placeholder)
  🗞️ 주간 인사이트                 ← 준비 중 (placeholder)

크리에이티브
  🗂️ 전체                         ← 경쟁사 대시보드 메인 (KPI+매트릭스+바로가기+전체 소재)
  📘 메타 / 🔎 구글                ← 매체별 상세 필터+그리드
  ⭐ 즐겨찾기                      ← 폴더별로 즐겨찾기한 소재 모아보기

모니터링
  🖼️ 브랜드검색                    ← 네이버 브랜드검색 PC/MO 스크린샷+세트매칭
  검색광고 일반키워드              ← 네이버 파워링크("증권"/"주식" 키워드 경매)
  🏷️ 검색광고 브랜드키워드         ← 브랜드명 자체를 키워드로 한 파워링크

트렌드
  📈 검색어 트렌드                 ← 데이터랩 검색량 + 코스피/코스닥/나스닥
  📊 트렌드 리포트                 ← 구글 시트 연동(앱별 MAU/신규설치)
  💬 커뮤니티 반응                 ← 디시/펨코 + Claude 웹서치 기반 화제 키워드
```
"경쟁사 대시보드 - 브랜드별"은 사이드바에 별도 항목이 없고, "전체" 탭의 "경쟁사 바로가기"에서
브랜드를 클릭하면 그 브랜드의 전체 소재(개수 제한 없음)를 보여주는 화면으로 진입합니다.

### 스크래핑 방식 및 진행 시점 (요약)
모든 수집은 `ad-collector/scheduler.js`가 Windows 작업 스케줄러에 상시 등록되어 자동으로
돌립니다(팀원 PC가 켜져 있는 동안). 사람이 매번 실행 버튼을 누르는 구조가 아닙니다.

| 무엇을 | 어떻게(스크래핑 방식) | 언제 | 과금 여부 |
|---|---|---|---|
| Meta 광고("증권" 키워드) | Apify API | 주 1회, 이번 주 첫 평일 11시 | **과금 있음** (유일하게 과금되는 수집) |
| Meta 브랜드 9개 순환 확인 | Playwright | 2시간마다(짝수 시) | 과금 없음 |
| Meta 이미지/영상 채우기 | Playwright 상세페이지 방문 | 2시간마다(홀수 시) | 과금 없음 |
| Google 광고(브랜드 9개) | Playwright (광고 투명성 센터) | 주 1회, 이번 주 첫 평일 11시 | 과금 없음 |
| 네이버 파워링크(일반키워드) | Playwright | 월/수/금 11시 | 과금 없음 |
| 네이버 브랜드검색 | Playwright + 스크린샷 | 주 1회(평일 13시) | 과금 없음 |
| 파워링크 브랜드키워드 | Playwright, 브랜드×기기 새로고침 30회 | 주 1회(평일 13시) | 과금 없음 |
| 코스피/코스닥/나스닥 지수 | Yahoo Finance API | 매일 09시 | 과금 없음 |
| 트렌드 리포트 | 구글 시트 연동 | 매일 09시 30분 | 과금 없음 |
| **커뮤니티 반응**(디시+펨코+Claude 웹서치) | Playwright 수집 + Claude CLI | **매일 08시** | Claude 구독 사용량 소모(API과금 아님) |
| 구글 이미지 다운로드 실패분 복구 | `googleMediaBackfill.js` | 매일 07시 | 과금 없음 |
| 구글 이미지 OCR 텍스트 백필 | `googleTextBackfill.js`(200건/일) | 매일 07시 30분 | Claude 구독 사용량 소모 |
| 구글 마지막 게재일 백필 | `googleLastShownBackfill.js` | 매일 07시 45분 | 과금 없음 |
| 날짜 없는 구글 소재 2025년 추정치 채우기 | `noDateFallbackBackfill.js` | 매일 07시 50분 | 과금 없음 |

공휴일이면 주 1회짜리(메타/구글)는 그 주의 다음 평일로 자동 밀리고, 나머지는 그날만
건너뜁니다(자세한 로직은 위 "수집 대상 및 주기" 섹션 참고). `sync-data.js`는 스케줄에
포함되어 있지 않아서, 수집된 내용을 대시보드(로컬/Vercel)에 반영하려면 수동으로 실행하고
Vercel에 반영하려면 추가로 git commit/push까지 해야 합니다(위 "3. 서버 배포 방법" 참고).

### 탭별 기능 한 줄 정리
- **전체**: 매체 불문 전체 소재. 상단 KPI + 브랜드별 매트릭스 + "경쟁사 바로가기"(브랜드
  클릭 시 그 브랜드 전체 소재로 이동) + Claude 소재 인사이트 + 검색/기간/광고주 슬라이서.
  화면엔 최근 150개만 그리고(성능), 브랜드 상세로 들어가면 전체 다 보임. "최신순/게재일
  긴 순서" 정렬과 "비슷한 소재 접기" 토글 있음.
- **메타 / 구글**: 각 매체만 따로, 기간(시작~종료일) 필터와 광고주 슬라이서가 추가로 있음.
- **즐겨찾기**: 즐겨찾기한 소재를 폴더별로 모아봄.
- **브랜드검색**: 네이버 브랜드검색 PC/MO 스크린샷 + 랜딩페이지 버튼 클릭 결과. A/B
  로테이션 감지 + PC/MO 세트 자동 매칭(위 "대시보드 탭별 기능" 섹션 참고).
- **검색광고 일반키워드**: "증권"/"주식" 파워링크 경매 결과 그대로(경쟁사 아닌 광고주도
  섞여 나오는 게 정상).
- **검색광고 브랜드키워드**: 브랜드명 자체로 검색해서 그 브랜드 본인 광고만 필터링, 30회
  새로고침으로 로테이션 소재 누적.
- **검색어 트렌드**: 데이터랩 검색량 + 지수, 결과에 대한 Claude 인사이트.
- **트렌드 리포트**: 팀 구글 시트(앱별 MAU 등) 그대로.
- **커뮤니티 반응**: 매일 화제 키워드 TOP15(시장 전체/메리츠증권 특정) + 감정분류 +
  실제 반응 인용, 캘린더로 과거 조회, 전일比/7일 추이.
- **데일리 뉴스 / 주간 인사이트**: 아직 구현 전(placeholder).
